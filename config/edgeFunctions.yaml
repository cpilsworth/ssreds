kind: "EdgeFunctions"
version: "1"
data:
  services:
    - name: ssreds
  # Uncomment to provision a config value at deploy time. Locally this comes
  # from fastly.toml [local_server.config_stores.config_default].
  # config:
  #   - key: EDS_ORIGIN
  #     value: ${{ EDS_ORIGIN }}
